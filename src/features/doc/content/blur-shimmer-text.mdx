---
title: Blur Shimmer Text
description: A text component that loops phrases with a horizontal blur shimmer.
image: /images/components/blur-shimmer-text.png
category: components
new: true
createdAt: 2026-05-22
updatedAt: 2026-05-23
---

<ComponentPreview
  name="blur-shimmer-text-demo"
  openInV0Url="https://ratneshc.com/r/blur-shimmer-text.json"
  canReplay
/>

## Installation

<CodeTabs>

<TabsListInstallType />

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ratneshc.com/r/blur-shimmer-text.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install clsx tailwind-merge motion
```

<Step>Add a cn helper.</Step>

<ComponentSource
  src="src/lib/utils.ts"
  title="lib/utils.ts"
  showLineNumbers
  collapsible="false"
/>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="src/registry/components/blur-shimmer-text/blur-shimmer-text.tsx"
  title="components/blur-shimmer-text.tsx"
  showLineNumbers
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { BlurShimmerText } from "@/components/blur-shimmer-text";
```

```tsx
<BlurShimmerText texts={["Text 1", "Text 2"]} />
```

## API Reference

| Prop         | Type                                                                            | Default             | Description                             |
| :----------- | :------------------------------------------------------------------------------ | :------------------ | :-------------------------------------- |
| `texts`      | `string[]`                                                                      | -                   | Phrases for the blur-shimmer-text loop. |
| `as`         | `motion.p \| motion.span \| motion.code \| motion.h1 \| motion.h2 \| motion.h3` | `motion.p`          | Element for the active phrase.          |
| `className`  | `string`                                                                        | -                   | Classes for the blur-shimmer-text root. |
| `interval`   | `number`                                                                        | `2`                 | Seconds before switching phrases.       |
| `blur`       | `number`                                                                        | `6`                 | Shimmer blur in pixels.                 |
| `transition` | `Transition`                                                                    | `{ duration: 0.5 }` | Per-character motion transition.        |
| `variants`   | `Variants`                                                                      | -                   | Custom variants; overrides `blur`.      |
